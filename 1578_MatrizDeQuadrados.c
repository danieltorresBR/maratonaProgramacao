#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#define linha 3
#define coluna 3


int main(){

	int i, j, n, k, a[h][m﻿﻿];

	scanf("%d", &n);

	scanf("%d", &m);


	for (k = 0; k < n; k++){
		for(l=0;l<linha;l++){
      printf("\nLinha %i\n", l+1);
      for(c=0;c<coluna;c++){
        printf("coluna %i: ", c+1);
        scanf("%d", &mesas[l][c]);
      }
    }
	}
	return 0;
}
